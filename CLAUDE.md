# 프로젝트 규칙

## 사용자 작업 데이터를 절대 건드리지 않는다

이 프로젝트의 대시보드(`에듀코스트.html`)는 사용자의 실제 설계내역서 작업 내용을
브라우저 localStorage에 보관한다. 다음 키는 **읽기만** 하고 절대 쓰거나 지우지 않는다.

- `EDUCOST-설계내역서-대시보드-v1` — 내역서 본체, 셀 수정, 삭제 행, 요율 변경
- `educost-takeoff-standalone-v1` — 물량산출
- `EDUCOST-대시보드-*` — 화면 설정

테스트할 때 지켜야 할 것:

- `save()`를 호출하지 않는다.
- `OV` / `FOV` / `DEL` / `ADD` / `WB` 를 덮어쓰지 않는다. 값을 바꿔 확인해야 하면
  원래 값을 저장해 두었다가 즉시 되돌리고, `save()`는 부르지 않는다.
- 버튼을 일괄 클릭해 훑는 방식(`querySelectorAll('button').forEach(b=>b.click())`)을
  쓰지 않는다. 「내역서로 넣기」, 「전체 내용 비우기」, 「수량 모두 0」 같은 파괴적
  동작이 섞여 있다.
- `renderSheet()` 는 `recalc()` 만 하고 저장하지 않으므로 안전하다.

기능 검증이 필요하면 사용자 데이터가 없는 사본(`deploy/index.html`)을 열어서 한다.

## 배포본 — 본체를 고치면 함께 갱신한다

| 경로 | 용도 | 만드는 법 |
|---|---|---|
| `에듀코스트-단독본.html` | 배포물 — 업무망용, 파일 하나로 실행 | deploy에서 식별정보 치환 후 표준 일위대가 JSON 3종 내장 |
| `deploy/index.html` | 배포물 아님 — `run_script`가 읽을 수 있는 본체 사본 | 본체를 그대로 복사 |

Netlify 배포는 폐기했다(업무망 차단·사용 안 함). 다시 만들지 않는다.

식별정보 치환 대상 세 곳:
- `<div class="iorg">전남광주통합특별시 신안교육지원청</div>` → `학교시설 설계내역서`
- `[ 도초고 본관동 외 2동 누수 보수공사 ]` (내장 WB 안 4곳) → `[  ]`
- `org:'전남광주통합특별시신안교육지원청'` → `org:''`

## 파일 이름

`run_script`는 한글 파일명을 읽지 못한다. 본체를 스크립트로 다뤄야 하면
`deploy/index.html` 사본을 소스로 쓴다.

## 업무망 제약

사용자 업무망에서 외부 사이트(Netlify 등)와 AI API가 차단된다.
새 기능은 외부 접속 없이 동작하는 방식을 우선한다.

## 버전 표기

상단 버튼 줄 아래 `<div id="appVer">v1.0</div>` 가 앱 버전이다.
**재배포(단독본 재생성)마다 0.1씩 올린다** — index.html, deploy/index.html, 에듀코스트-단독본.html 세 곳을 같은 값으로 맞춘다.

- 현재 버전: v1.0 (2026-09-17)
